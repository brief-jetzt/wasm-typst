{
  "name": "wasm-typst-glue",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "wasm-pack build && tsdown",
    "test": "tsc && wasm-pack test --chrome --firefox --headless",
    "ci": "oxlint --type-aware --deny-warnings -f github",
    "format": "oxfmt",
    "lint": "oxlint --type-aware",
    "lint:fix": "oxlint --type-aware --fix",
    "typecheck": "tsc"
  },
  "devDependencies": {
    "oxfmt": "^0.65.0",
    "oxlint": "^1.80.0",
    "oxlint-tsgolint": "^7.0.2001",
    "tsdown": "^0.22.14",
    "typescript": "^7.0.2",
    "wasm-pack": "^0.15.0"
  },
  "version": "0.0.2"
}
